{{> header }}

<div class="columns">
    <div class="column is-one-third">
        <h1>Gallery</h1>
        <ul>
            {{#each drawings}}
            <li>
                <a href="/{{this.username}}/drawing/{{this.id}}">{{this.name}}</a>
            </li>
            {{/each}}
        </ul>
    </div>
</div>